# === Core ===
anthropic>=0.40.0,<1.0.0
boto3>=1.34.0,<2.0.0          # AWS Bedrock 듀얼 프로바이더 (선택적)
langgraph>=0.2.0,<1.0.0
langchain-core>=0.3.0,<0.4.0  # minor 버전 간 breaking 변경 잦음 — 타이트하게 고정
langsmith>=0.2.0,<1.0.0       # LangSmith 에이전트 트레이싱
openai>=1.0.0,<2.0.0          # OpenAI API 프로바이더
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0        # C 확장 포함 — ABI 호환성 보장 필요

# === Database ===
PyMySQL>=1.1.0,<2.0.0
pinecone>=3.0.0,<4.0.0
neo4j>=5.0.0,<6.0.0

# === API ===
fastapi>=0.110.0,<1.0.0
uvicorn>=0.30.0,<1.0.0
httpx>=0.27.0,<1.0.0

# === Image ===
Pillow>=10.0.0,<12.0.0        # C 확장 포함 — ABI 호환성 보장 필요

# === Monitoring ===
python-json-logger>=2.0.0,<4.0.0  # C-1: 구조화 JSON 로깅
prometheus-client>=0.20.0,<1.0.0  # C-2: Prometheus 메트릭

# === Utils ===
pyyaml>=6.0.0,<7.0.0
pdfplumber>=0.11.0,<1.0.0    # PDF 텍스트 추출 (Phase 1 적재 스크립트용)
